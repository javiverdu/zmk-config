/* in config/corne.overlay */

/*
 * Find the node labeled 'ssd1306_display' (which the
 * zmk-nice-oled module created) and override its height.
 */
&ssd1306_display {
    /* This is the fix: change height from 32 to 64 */
    height = <64>;
};
